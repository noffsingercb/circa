// Circa is a static single-page bundle. There is no server to render on and
// nothing to prerender per-route: the whole app is one client-side page.
export const prerender = true;
export const ssr = false;
